exports.uselessStars = function uselessStars() {
    log(chalk.cyan("********************************************************************************"));
    log(chalk.cyan("********************************************************************************"));
    log(chalk.cyan("********************************************************************************"));
    log(chalk.cyan("********************************************************************************"));
    log(chalk.cyan("********************************************************************************"));
}
